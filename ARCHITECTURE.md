# Architecture Overview - v6.0.4 BANGER UPDATE

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         ARTHEMIS CONTROL                         │
│                    Professional Edition v6.0.4                   │
└─────────────────────────────────────────────────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
        ┌───────────▼──────────┐  ┌──────────▼──────────┐
        │   Main Application   │  │  Advanced Features  │
        │     (MainWindow)     │  │      (New! 🔥)      │
        └──────────┬───────────┘  └──────────┬──────────┘
                   │                         │
        ┌──────────┴──────────┐   ┌─────────┴──────────┐
        │                     │   │                     │
   ┌────▼─────┐  ┌──────────▼┐  ┌▼──────────┐  ┌──────▼──────┐
   │ Anti-    │  │ Aim       │  │ Macro     │  │ Button      │
   │ Recoil   │  │ Assist    │  │ System    │  │ Remapping   │
   └──────────┘  └───────────┘  └───────────┘  └─────────────┘
                                       │                │
                              ┌────────┴────────┐  ┌────┴────┐
                              │                 │  │         │
                         ┌────▼────┐  ┌────────▼──▼───┐  ┌──▼──────┐
                         │ Rapid   │  │ Combo         │  │ Turbo   │
                         │ Fire    │  │ Detection     │  │ Mode    │
                         └─────────┘  └───────────────┘  └─────────┘
                                              │
                    ┌─────────────────────────┴─────────────────────┐
                    │                                               │
            ┌───────▼────────┐                            ┌─────────▼────────┐
            │   Controller   │                            │   Virtual        │
            │   Input        │◄───────────────────────────┤   Controller     │
            │   (Physical)   │                            │   (ViGEm)        │
            └────────────────┘                            └──────────────────┘
```

## Component Breakdown

### Core Systems (Existing)
```
┌──────────────────────────────────────────┐
│ MainWindow.xaml.cs                       │
├──────────────────────────────────────────┤
│ ✓ Anti-Recoil System                     │
│ ✓ Aim Assist (6 modes)                   │
│ ✓ Rapid Fire                             │
│ ✓ Auto Ping                              │
│ ✓ Controller Detection                   │
│ ✓ ViGEm Integration                      │
│ ✓ Profile Management                     │
└──────────────────────────────────────────┘
```

### Advanced Features (NEW! 🔥)
```
┌──────────────────────────────────────────┐
│ MacroSystem.cs                           │
├──────────────────────────────────────────┤
│ ⚡ Recording                             │
│ ⚡ Playback                              │
│ ⚡ Looping                               │
│ ⚡ Optimization                          │
│ ⚡ Import/Export                         │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ ButtonRemappingSystem.cs                 │
├──────────────────────────────────────────┤
│ 🎯 Per-Profile Mappings                 │
│ 🎯 Modifier Support                     │
│ 🎯 Quick Swap                           │
│ 🎯 Turbo Mode                           │
│ 🎯 Stick Inversion                      │
└──────────────────────────────────────────┘

┌──────────────────────────────────────────┐
│ ComboSystem.cs                           │
├──────────────────────────────────────────┤
│ ⚔️ Real-time Detection                  │
│ ⚔️ Fighting Game Combos                 │
│ ⚔️ Quick-Tap Detection                  │
│ ⚔️ Action Execution                     │
│ ⚔️ Statistics Tracking                  │
└──────────────────────────────────────────┘
```

### UI Components
```
┌──────────────────────────────────────────┐
│ AdvancedFeaturesWindow.xaml              │
├──────────────────────────────────────────┤
│ 📋 Macro Manager Tab                    │
│ 🎯 Button Remapping Tab                 │
│ ⚔️ Combo System Tab                     │
│ 📊 Statistics Tab                       │
└──────────────────────────────────────────┘
```

## Data Flow

### Input Processing Pipeline
```
Physical Controller Input
         │
         ▼
┌────────────────────┐
│ HID/XInput Reader  │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Combo Detection    │───► Combo Detected? ───► Execute Action
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Button Remapping   │───► Apply Mapping
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Macro Recording?   │───► Record Action
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Feature Processing │
│ (AR/AA/RF/etc.)    │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Virtual Controller │
│    (ViGEm)         │
└────────────────────┘
```

### Macro Playback Flow
```
User Triggers Macro
         │
         ▼
┌────────────────────┐
│ Macro System       │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Read Action List   │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Execute Button     │───► Virtual Controller
│ Press/Release      │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Wait Delay         │
└────────┬───────────┘
         │
         ▼
┌────────────────────┐
│ Next Action?       │───► Yes: Loop Back
└────────┬───────────┘
         │ No
         ▼
      Complete
```

## File Structure

```
Trident.MITM.App/
│
├── Core Files
│   ├── MainWindow.xaml               (Main UI)
│   ├── MainWindow.xaml.cs            (Core logic)
│   ├── MainWindow.Extensions.cs      (Extensions & helpers)
│   ├── MainWindow.Features.cs        (Feature implementations)
│   ├── App.xaml                      (Application)
│   └── App.xaml.cs                   (App logic)
│
├── Advanced Features (NEW! 🔥)
│   ├── MacroSystem.cs                (Macro engine)
│   ├── ButtonRemappingSystem.cs      (Remapping engine)
│   ├── ComboSystem.cs                (Combo detection)
│   ├── AdvancedFeaturesWindow.xaml   (Advanced UI)
│   ├── AdvancedFeaturesWindow.xaml.cs (UI logic)
│   └── AdvancedFeaturesIntegration.cs (Integration layer)
│
├── Support Files
│   ├── BezierCurve.cs                (Curve calculations)
│   ├── KeyAuth.cs                    (Authentication)
│   ├── LocalizationManager.cs        (Multi-language)
│   ├── ExceptionHandler.cs           (Error handling)
│   └── RumbleForwarder.cs            (Haptics)
│
├── Windows
│   ├── AboutWindow.xaml              (About dialog)
│   ├── LicenseWindow.xaml            (License dialog)
│   ├── OverlayWindow.xaml            (In-game overlay)
│   ├── RecoilDrawWindow.xaml         (Recoil pattern editor)
│   └── ToolsWindow.xaml              (System tools)
│
├── Configuration
│   ├── trident.json                  (Main config)
│   ├── macros.json                   (Macro library) ⚡NEW
│   ├── button_profiles.json          (Button configs) ⚡NEW
│   └── combos.json                   (Combo configs) ⚡NEW
│
└── Documentation
    ├── README.md                     (Main documentation)
    ├── ADVANCED_FEATURES.md          (Features guide) ⚡NEW
    ├── QUICK_REFERENCE.md            (Quick reference) ⚡NEW
    ├── USER_GUIDE.md                 (User manual)
    ├── FAQ.md                        (FAQ)
    ├── INSTALLATION.md               (Setup guide)
    └── VISUAL_ENHANCEMENTS.md        (UI guide)
```

## Technology Stack

```
┌─────────────────────────────────────┐
│          Application Layer          │
├─────────────────────────────────────┤
│ WPF (Windows Presentation Foundation)
│ XAML UI                             │
│ C# .NET 8.0                         │
└─────────────────────────────────────┘
                 │
┌────────────────┴───────────────────┐
│         Framework Layer            │
├────────────────────────────────────┤
│ .NET 8.0 Runtime                   │
│ System.Text.Json (Serialization)   │
│ System.Windows.Media (Animations)  │
└────────────────────────────────────┘
                 │
┌────────────────┴───────────────────┐
│         Library Layer              │
├────────────────────────────────────┤
│ Nefarius.ViGEm.Client (Virtual Controllers)
│ HidSharp (Controller I/O)          │
│ SharpDX.XInput (XInput)            │
└────────────────────────────────────┘
                 │
┌────────────────┴───────────────────┐
│         System Layer               │
├────────────────────────────────────┤
│ ViGEm Bus Driver                   │
│ Windows API (User32, Kernel32)    │
│ HID Subsystem                      │
└────────────────────────────────────┘
```

## Event System

```
┌──────────────────────────────────────────┐
│           Event Publishers               │
└──────────────┬───────────────────────────┘
               │
     ┌─────────┼─────────┐
     │         │         │
┌────▼───┐ ┌──▼────┐ ┌──▼────────┐
│ Macro  │ │Button │ │  Combo    │
│ Events │ │ Events│ │  Events   │
└────┬───┘ └──┬────┘ └──┬────────┘
     │        │         │
     │        │         │
     └────────┼─────────┘
              │
     ┌────────▼───────────┐
     │  Event Subscribers │
     ├────────────────────┤
     │ • UI Updates       │
     │ • Logging          │
     │ • Statistics       │
     │ • Action Execution │
     └────────────────────┘
```

## Memory Management

```
Static Components (Always Loaded):
├── MacroSystem             (~5 KB)
├── ButtonRemappingSystem   (~3 KB)
├── ComboSystem             (~4 KB)
└── MainWindow              (~50 KB)

Dynamic Components (As Needed):
├── Macro Library           (varies, ~100 KB typical)
├── Button Profiles         (varies, ~50 KB typical)
├── Combo Configurations    (varies, ~30 KB typical)
└── Advanced Features UI    (~200 KB when open)

Total Typical Memory: ~450 KB
Maximum Expected: ~2 MB
```

## Performance Metrics

```
Input Processing:
├── Combo Detection         < 1ms per input
├── Button Remapping        < 0.5ms per input
├── Macro Recording         < 0.3ms per action
└── Total Input Latency     < 2ms

Macro Playback:
├── Action Execution        < 1ms per action
├── Timing Accuracy         ±5ms
└── Loop Performance        No degradation

Configuration:
├── Load Time               < 100ms
├── Save Time               < 50ms
├── Export/Import           < 200ms
└── File Size               < 100 KB (typical)
```

## Security Architecture

```
┌──────────────────────────────────────┐
│        Input Validation              │
├──────────────────────────────────────┤
│ • Button name validation             │
│ • Timing range checks                │
│ • Profile name sanitization          │
│ • JSON schema validation             │
└──────────────────────────────────────┘
                │
┌───────────────▼──────────────────────┐
│        Error Handling                │
├──────────────────────────────────────┤
│ • Try-catch blocks                   │
│ • Graceful degradation               │
│ • User-friendly error messages       │
│ • Logging system                     │
└──────────────────────────────────────┘
                │
┌───────────────▼──────────────────────┐
│        Data Protection               │
├──────────────────────────────────────┤
│ • Local storage only                 │
│ • No telemetry                       │
│ • Encrypted configurations (optional)│
│ • Backup system                      │
└──────────────────────────────────────┘
```

---

**Version**: 6.0.4 BANGER UPDATE 🔥
**Architecture**: Event-Driven, Modular, Extensible
**Design Pattern**: MVVM (Model-View-ViewModel)
**Performance**: Optimized for real-time gaming
